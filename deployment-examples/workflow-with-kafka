1. sudo sh kind-with-registry.sh
2. kubectl create namespace kafka
3. kubectl apply -f kafka-operator.yaml -n kafka
4. kubectl logs deployment/strimzi-cluster-operator -n kafka -f
5. sudo kubectl apply -f kafka-persistent-single.yaml -n kafka
6. sudo kubectl wait kafka/edna-cluster --for=condition=Ready --timeout=300s -n kafka 
7. docker tag ebka-twitter-streamer:latest localhost:5000/ebka-twitter-streamer:latest
8. docker push localhost:5000/ebka-twitter-streamer:latest


docker pull joxit/docker-registry-ui:latest
docker run -d -p 80:80 joxit/docker-registry-ui

visit localhost:80
add http://localhost:5000
will need extension https://addons.mozilla.org/en-US/firefox/addon/cors-everywhere/ on firefox
or extension https://mybrowseraddon.com/access-control-allow-origin.html on chrome or edge
docker kill <container-id for joxit...>

kubectl run ebka-twitter --rm -i --tty --image localhost:5000/ebka-twitter-streamer:latest -- sh
(press enter once to see shell #, if it is not visible)


sudo kubectl -n default run kafka-consumer -ti --image=strimzi/kafka:0.19.0-kafka-2.5.0 --rm=true --restart=Never -- bin/kafka-console-consumer.sh --bootstrap-server edna-cluster-kafka-bootstrap.kafka.svc.cluster.local:9092 --topic test-topic --from-beginning



---

1. cd into the directory (e.g. TwitterSampledStreamerToKafka)
2. j2 ../Dockerfile.jinja2 config.yaml > Dockerfile
3. Create the dockerignore
4. copy the development package into the directory (i.e. python/ebka/): 
    cp -r ../../python/edna/src .
    cp ../../python/edna/setup.py .
    cp ../../python/edna/setup.cfg .
5. docker build -t edna-twitter-streamer-kafka:latest .
6. docker tag edna-twitter-streamer-kafka:latest localhost:5000/edna-twitter-streamer-kafka:latest
7. docker push localhost:5000/edna-twitter-streamer-kafka:latest


sudo kubectl -n default run kafka-consumer -ti --image=strimzi/kafka:0.19.0-kafka-2.5.0 --rm=true --restart=Never -- bin/kafka-console-consumer.sh --bootstrap-server edna-cluster-kafka-bootstrap.kafka.svc.cluster.local:9092 --topic twitter-sampled-stream --from-beginning

kubectl run edna-twitter --rm -i --tty --image localhost:5000/edna-twitter-streamer-kafka:latest -- sh